name: Nginx App
on: 
 push: 
  branches: [main]

jobs:
  build:
    runs-on: ububuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Build image
        run:  docker build -t nginxapp: v1

        


        
