name: Environmnet Variable Secrets
on: 
 push:
   

jobs:
  variable:
    runs-on: ubuntu-latest
    steps:
      - name: Passing variables
        env:
          VAR: ${{ secrets.MY_SECRET_MESSAGE }}
        run: |
          echo "$VAR" > envfile.txt

          


            
          

    
    
  
